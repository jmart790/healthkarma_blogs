import axios from '~/plugins/axios'

/**
 *
 *  @functions are acynchronous so we can await for the response and
 *             extract the data object which will always contain:
 *  @status FAILED || ERROR || SUCCESS,
 *  @message "a message based upon the status from the back end",
 *  @data an object containing information upon success
 */
export default {

}
